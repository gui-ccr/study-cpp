#include <iostream>
#include <cmath>

main(){
     
    double x = 2.25;
    double y = 3;
    double z;

    // z = std::max(x,y); comparativo do valor maximo
    // z = std::min(x,y); comparativo valor minimo
    // z = pow(x,y);  power = elevado x ^ y  
    // z = sqrt(x); raiz quadrada
    // z = abs(-3); exibe o valor absoluto ou seja o positivo 
    // z = round(x); arredonda o valor 
    // z = ceil(x);  arredonda para cima sempre
    // z = floor(x); arredonda sempre para baixo 

    // tem muito mais qualquer coisa é so procurar na documentação do cmath no c++

    std::cout << z;


}